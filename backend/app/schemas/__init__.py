"""Pydantic v2 schemas for the /api/v1 surface (docs/05 §5).

Conventions: money is whole-tenge ``int``, months are ``YYYY-MM`` strings,
timestamps are UTC, ids are UUID (patients: hash strings).
"""
