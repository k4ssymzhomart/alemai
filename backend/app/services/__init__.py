"""Service layer — one package per pillar module (docs/05 §3).

Each subpackage docstring states its behavior contract; implementations land
behind those contracts without changing the API layer.
"""
